<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>List of Flights</title>
</head>
<body>
  <h1>List of Flights</h1>

  <!-- Navigation Bar -->
  <ul>
    <li><a href="/">Home</a></li>
    <li><a href="/flights/new">Add Flight</a></li>
  </ul>

  <!-- Flight List -->
  <ul>
    <% for (let i = 0; i < flights.length; i++) { %>
      <li>
        <strong>Airline:</strong> <%= flights[i].airline %><br>
        <strong>Airport:</strong> <%= flights[i].airport %><br>
        <strong>Flight Number:</strong> <%= flights[i].flightNo %><br>
        <strong>Departure Date/Time:</strong> <%= flights[i].departs %>
        <a href="/flights/<%= flights[i]._id %>">Detail</a>
      </li>
    <% } %>
  </ul>

  <!-- Function to format date/time -->
  <%- include('./partials/footer') %>
</body>
</html>
